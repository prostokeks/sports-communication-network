<%= @room.title %>
<%= @room.content %>

<%= link_to "Edit", edit_room_path(@room) %>
<%= link_to "Delete", room_path(@room), data: { "turbo-method": :delete,
                                                turbo_confirm: "You sure?" } %>
<%= link_to "Back", rooms_path %>
<%= render "chats/chat_block" %>